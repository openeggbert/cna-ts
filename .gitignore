# Node/npm and generated package output
/node_modules/
/dist/
/coverage/
/build/
*.log
*.tgz

# Native/WebAssembly CNA artifacts are packaged output, not source
/runtimes/
*.dll
*.dylib
*.so
*.wasm

# Editors and operating systems
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
content/dist/
content/node_modules/

# Headers extracted at the CI-pinned CNA revision, so the checked-in reports can be regenerated
# against the same revision CI compares them to rather than against local cnanext HEAD.
.cna-headers-pin/
