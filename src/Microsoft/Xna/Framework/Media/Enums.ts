export enum MediaSourceType {
  LocalDevice = 0,
  WindowsMediaConnect = 4,
}

export enum MediaState {
  Stopped = 0,
  Playing = 1,
  Paused = 2,
}

export enum VideoSoundtrackType {
  Music = 0,
  Dialog = 1,
  MusicAndDialog = 2,
}
