export enum AudioChannels {
  Mono = 1,
  Stereo = 2,
}

export enum AudioStopOptions {
  AsAuthored = 0,
  Immediate = 1,
}

export enum MicrophoneState {
  Started = 0,
  Stopped = 1,
}

export enum SoundState {
  Playing = 0,
  Paused = 1,
  Stopped = 2,
}
