# Runtime capability and blocker inventory

Profile: **XNA 4.0 Windows runtime**

Selected evidence environment: **Linux x86-64 Node with CNA ABI 0.21.0 HEADLESS renderer and NULL audio, plus headless Chromium with the CNA ABI 0.21.0 WEBGL2 WebAssembly artifact**

Each row is a reviewed operation family; overloads with the same implementation and evidence share a row.
This inventory is independent of the strict API verifier: API shape completeness does not imply runtime capability.

## Explicit failure-site audit

All selected-profile framework files containing explicit NativeUnavailableError or NotSupportedException construction were reviewed into the operation families below. Internal construction guards share their public operation family: the seventh NotSupportedException site is Texture2D's internal whole-level byte upload, which belongs to the CNB Texture2D family that uses it. The count fell by one when VideoPlayer.GetTexture stopped refusing outright and began projecting CNA's borrowed frame. It rose by one when Texture3D gained the adopting construction the colour grade's volume LUT arrives through.

- NativeUnavailableError construction sites: 62
- NotSupportedException construction sites: 7
- Framework files containing those sites: 23

## Counts

| Category | Operation families |
| --- | ---: |
| VERIFIED_MANAGED | 21 |
| VERIFIED_NATIVE | 91 |
| VERIFIED_WEBASSEMBLY | 13 |
| EXPLICITLY_UNAVAILABLE_WITH_CURRENT_BACKEND | 5 |
| UPSTREAM_CNA_BLOCKED | 5 |
| FIXTURE_PENDING | 3 |
| HARDWARE_PENDING | 6 |
| PLATFORM_PENDING | 3 |
| UNIMPLEMENTED_CNA_TS | 2 |
| LANGUAGE_MAPPING_LIMITATION | 3 |
| NOT_APPLICABLE_TO_SELECTED_ENVIRONMENT | 1 |

## VERIFIED_MANAGED

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Audio enums, listener/emitter values and SoundEffect duration/size arithmetic | CNA-TS | 47 deterministic XNA differential observations |
| BlendState, DepthStencilState, RasterizerState and SamplerState managed values/presets | CNA-TS | graphics-foundation unit suite verifies snapshots, defaults and disposal |
| Content cache, failure cleanup, dependency-ordered Unload and Dispose | CNA-TS | content and content-xnb lifecycle tests including failed nested construction and native Effect-to-texture retention |
| ContentManager LZX-compressed XNB framing and decompression | CNA-TS | synthetic single/multi-frame tests plus exact independent 16,561/44,032-byte fixture comparison |
| ContentManager uncompressed XNB framing, reader tables, object dispatch and shared resources | CNA-TS | deterministic synthetic content-xnb fixtures |
| ContentReader.ReadExternalReference and nested ContentManager resolution | CNA-TS | relative/nested/repeated/circular/missing/malformed/compressed reference tests |
| Design TypeConverter projections and GamerServicesComponent lifecycle | CNA-TS | design and framework unit suites |
| Effect reflection, parameter values, techniques and managed stock-effect state | CNA-TS | graphics/content differential observations and graphics-foundation tests |
| Extended input's data path: joystick arrays and haptic answers, through the boundary | cna-ts/extensions/input | test/extensions-input.test.mjs covers what a host with no joystick cannot. A deterministic backend supplies four state arrays of four different lengths with four different value sets, so substituting any one for any other is caught; a haptic device answers each of its four operations differently, so a projection returning one answer for all of them fails. This was written because a defect planted in the integration suite -- reading the hats out of the buttons -- stayed green there: with no device attached both arrays are empty. Three planted defects now fail it: hats from buttons, an exchanged trackball X and Y, and a Dispose that releases without closing. It is a projection test and claims nothing about physical hardware |
| Framework math, geometry, curves, packed vectors, TimeSpan, Color and value contracts | CNA-TS | 83 deterministic XNA differential observations plus math/geometry/graphics-value unit suites |
| GameComponent, DrawableGameComponent, collections, services and managed events | CNA-TS | framework-components unit suite covers ordering, mutation, filtering, services and disposal |
| GamerServices and Net declarations, identities, value shapes and exception hierarchy | CNA-TS | the xna40-windows-live profile holds at zero differences over 74 types and 676 members, with thirteen behaviour assertions over the identities, the catchable exception hierarchy, the empty signed-in collection, Guide title state, the mutable AvatarExpression, the null-preserving session property bag and a full packet round trip |
| Graphics presentation, viewport, display, vertex and render-target value objects | CNA-TS | graphics-foundation and graphics-values unit suites |
| KeyboardState, MouseState, GamePad values and Touch values/collections | CNA-TS | 23 deterministic XNA differential observations and input unit suite |
| Media collections, queue identity and MediaPlayer managed settings/events | CNA-TS | deterministic subsystem differential observations |
| Model graph collections, parent/child identity and absolute transforms | CNA-TS | graphics/content differential observation and managed Model tests |
| SoundEffectInstance managed lifecycle state and disposal guards | CNA-TS | differential and audio/media/storage design suites |
| SpriteBatch argument and Begin/End validation | CNA-TS | graphics/content differential observation and graphics-foundation tests |
| Storage path validation and isolated managed-directory adapter | CNA-TS | deterministic storage differential observation and design suite |
| Texture2D, SpriteFont and Model managed XNB reader graphs | CNA-TS | uncompressed and compressed synthetic reader-graph tests |
| Texture3D/TextureCube exact Color codec, bounds and CNA dispatch projection | CNA-TS | managed native-shaped backend verifies boxes, faces and exact packed Color round trips; all ABI signatures are compiler-audited and the qualified HEADLESS artifact returns documented NOT_SUPPORTED at creation |

## VERIFIED_NATIVE

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| A post-process pass being told why it could not do its work | cna-ts/extensions/graphics | the aerial-perspective pass never refuses: a frame missing a depth image, a far plane or the camera's matrices is copied through unchanged and the reason recorded in words. That ladder is qualified as a ladder rather than as one string -- with nothing supplied it says the depth image is missing, with depth and a far plane it says the camera matrices are, and with all three it says nothing at all -- so the reason is asserted to *change* as inputs are supplied, which a constant string could not do. Finding this also closed a real gap in this binding: the post-process frame carried no camera at all, so the passes that rebuild a world position out of the depth image could never do their work through it. It now carries the projection, both inverses, the previous frame's view-projection and whether there was one, each optional and each defaulting to what CNA's own initializer writes |
| An effect pass: a caller's own Effect run over a frame, borrowed or taken | cna-ts/extensions/graphics | two ownership rules, both measured rather than read. A borrowing pass leaves the effect the caller's: reading it four times returns it four times and detaches nothing, SetEffect(null) detaches and setting it back reattaches. An owning pass consumes the handle, which CNA does by releasing it out of its own registry, so the caller's Effect wrapper gives up its child views and stops owning anything, and its Dispose becomes the no-op Dispose always is once a resource has been given away -- established by measuring that CNA refuses the consumed handle with INVALID_HANDLE and that the technique and pass views release cleanly on either side of the consume. The getter is upstream finding 17: its header says the borrow must not be destroyed, and it mints a registered handle anyway, so obeying the header makes cna_game_destroy refuse for the rest of the process. The binding releases it and documents why on itself, and the windowed game's clean shutdown is the other half of that evidence |
| Atmospheric rendering: the drawn sky, the captured one, and the environment behind it | CNA-TS | the strongest acceptance in this package. CNA ships the scattering model twice -- once as the GLSL its sky shader runs and once as cna_atmospheric_sky_radiance, evaluated on the CPU without a device -- and ships a third route that says which way a screen point looks. So every texel of a drawn sky has a prediction assembled from two routes that never touch the GPU, and six skies of 1024 texels each are compared against it on a windowed OPENGLES3 renderer under Xvfb: nowhere do the shader and the model differ by more than one part in 255, which is the resolution the target has, and most texels are bit-identical. The picture is a gradient rather than a fill, a sun below the horizon is ten times darker, a sun placed inside the frustum makes that half of the image the bright one while a sun overhead lights both halves to within two per cent, halving the intensity halves every unclipped texel across the whole image, and a change of turbidity moves the drawn picture the same way and by the same amount as it moves the model -- which is asserted rather than a direction guessed here, because near the horizon aerosol both adds scattered light and takes away sunlight. The captured sky is checked without assuming a cube-map convention: six faces of six flat colours seen down six axes produce six different colours, each the middle of its own face and each of the six that were uploaded, and turning the sky a right angle about the up axis moves exactly the four equatorial faces and leaves the two poles. Intensity and tint multiply what was sampled, a skybox with no environment draws nothing and succeeds, and attaching it again brings the same sky back. The environment processor converts a panorama into a cube map, and handing that cube map over with SetOwnedEnvironment consumes it: the wrapper refuses further use and its Dispose becomes harmless. Two ownership traps were found by writing the binding to the header and measuring: cna_skybox_get_environment mints an owned handle whose loss makes the game undestroyable, and cna_render_pipeline_get_skybox documents the same contract and echoes the caller's own handle, so releasing it destroys their skybox -- upstream finding 15, with the binding doing the opposite thing in each case and saying why. Twenty-three planted binding defects fail and none survives |
| BasicEffect, AlphaTestEffect, DualTextureEffect, EnvironmentMapEffect and SkinnedEffect native construction | CNA-TS + CNA | all five distinct ABI-0.7 constructors return owned effects with real technique/pass views under qualified HEADLESS integration |
| BasicEffect, AlphaTestEffect, DualTextureEffect, EnvironmentMapEffect and SkinnedEffect native state/apply execution | CNA-TS + CNA | qualified HEADLESS integration synchronizes dependency-complete stock state, applies every stock effect and verifies clone, texture retention and deterministic disposal |
| Build-time content compilation: source files into .cnb, in a separate package | cna-ts-content | The direction docs/content-pipeline-boundary.md chose, built. `cna-ts-content` is a separate package with its own exports, and the runtime tarball contains none of it: the three routes in the whole binding that read a file by path live there and nowhere else, which is what keeps the browser runtime free of filesystem and compiler APIs. Bytes cross that boundary and handles do not -- each operation imports, describes, encodes and releases inside one native call -- so the package owns no native lifetime at all. Proved end to end: a PNG and a RIFF/WAVE file written from their own specifications (not produced by CNA, because an importer test in which CNA reads back its own output proves nothing) are compiled to .cnb, written to disk, and loaded by the real runtime, where the four exact texels and the exact sample bytes come back. The colour key is asserted at the texel: the keyed one becomes transparent and the other three are untouched. Wrong-format sources are refused in both directions, and a planted defect that accepted the colour key without applying it passed until the test stopped varying the content name as well |
| Camera frame capture | CNA-TS | opening the platform camera succeeds on a host that has none -- CNA documented contract -- and the state is what reports the absence: NotSupported with a 0x0 frame format. CNA own test backend then supplies what the hardware cannot: four distinct pixels published through it arrive in a caller-owned Texture2D with their channels the right way round, a second frame of sixteen different bytes replaces the first, a texture whose size does not match the frame is refused rather than resized, and the frame stays available until it is replaced. Every injectable state round-trips, and CNA refuses Closed and NotSupported for a device it opened. The injection hooks refuse a camera that is not the test backend, so a test cannot fabricate a reading for a real device. Three planted defects -- zeroed pixels, an acquisition that always claims success, and hooks that accept a real camera -- all fail. This is injection evidence, not hardware evidence: a physical camera stays hardware-pending |
| Cascade splits, spot cones and cube faces: the rest of the shadow-map maths | CNA-TS | a cascaded shadow map's split distances are checked against both closed forms rather than recorded numbers: lambda 0 is the uniform split exactly, lambda 1 the logarithmic one, and lambda 0.5 their midpoint at every cascade -- and the two schemes are asserted to disagree by more than tenfold near the camera, so those are not all the same check. CNA takes two to four cascades and refuses the rest with its own result code. A view frustum yields eight distinct corners and a bounding sphere that encloses them snugly -- the farthest corner is on the surface, not inside it, which is what stops a cascade shimmering as the camera turns. A spot light's view puts the light at the origin, its projection is perspective rather than orthographic, its range lands exactly on the far plane along the cone axis, and a wider outer angle changes the field of view. The six cube faces each put the light at the origin, each look along a unit axis, and form exactly three opposite pairs. The cube projection is ninety degrees and square. Three planted defects fail: a fixed lambda, a cube face that is always face zero, and a fixed cone angle |
| Clustered lighting: the assignment a GPU would read | CNA-TS | the three views of one assignment -- the per-cluster read, the flat index list and the per-cluster offsets -- are checked against each other for every one of sixty-four clusters, not a sample. The final offset equals the index list length equals the reported total reference count, the offsets never go backwards, and the reported maximum lights per cluster is the largest cluster actually produced. A per-cluster read that ignored its argument and an offset list missing its end marker were both planted and both fail |
| Clustered lighting: the cluster grid's exact geometry | CNA-TS | all sixty-four clusters of a 4x2x8 grid, each flat index checked against slice * tilesX * tilesY + y * tilesX + x rather than a sample. The depth axis is logarithmic and asserted against the closed form near * (far/near)^(slice/count), and separately asserted not to be the linear spacing, which would otherwise satisfy every weaker check. The projection round-trips: multiplying it by the inverse CNA returns gives the identity to within 1e-5 through XNA own Matrix.Multiply. A planted x/y swap in the index mapping fails this |
| Clustered lighting: the light set and what CNA refuses to light with | CNA-TS | three lights with three different values in every field, added, read back one at a time and in bulk, replaced, removed and cleared. A point light influence is a sphere on its own position with its own range; a spot influence is its cone bounding sphere, which is somewhere else entirely, and that difference is what shows CNA computing the bounds rather than copying the position -- a planted position-and-range substitution fails it. The two add paths are cross-checked: a point light added through CNA own conversion agrees with the same light built in the uniform shape on every field that has an effect and on the computed bounds, and differs only in the direction and cone angles a point light does not have, which CNA fills with its own canonical defaults rather than zero -- a rule this package hands to CNA rather than restating. CNA refuses a zero or negative range, a negative intensity and a spot whose inner angle is wider than its outer, each with INVALID_ARGUMENT |
| Clustered lighting: the shadow budget, and the refusals it counts | CNA-TS | two of three lights ask for a shadow map because only two carry the flag, so the request count is a property of the scene rather than of the budget. A budget of one grants one and counts one refusal; a budget of zero grants nothing and refuses every request. IsSelected and GetSelected name the same lights, a selected light has a positive score, and the hysteresis margin round-trips |
| CNA renderer identity and capability query extension | CNA-TS + CNA | qualified artifact reports HEADLESS/custom-effects/compiled-effects capability bits |
| CNA sensors: support enumeration and the accelerometer's absence path | cna-ts/extensions/sensors | the platform answers for all four sensor families, and on a host with no accelerometer the whole family agrees: the state is NotSupported, no data is valid, and CurrentValue refuses with InvalidOperationException rather than returning three zeroes a game would integrate into an orientation nothing measured. Start, Stop and the update interval report what the platform did rather than what was asked, disposal is idempotent, and a released sensor does not block the next one |
| CNB container: parse, validate, walk the table of contents and read chunk bytes | cna-ts/extensions/content | a container encoded by CNA's own writer parses back with its container version, asset type, schema version, CMET metadata and chunk order; a chunk's bytes are exact and its CRC-32C recomputes to the table-of-contents value; truncation, a flipped payload byte and an XNB are each refused with CNA_RESULT_IO, and an unknown mandatory chunk refuses the whole file |
| CNB curves and animation clips | cna-ts/extensions/content | A curve round-trips its two loop modes, its key count and every field of every key, including a continuity that differs between keys -- and then it is checked the way a curve actually matters, by evaluating both the authored and the decoded curve at ten positions including four between the keys, where the tangents and the continuity decide the answer. What comes back is a managed XNA Curve with no native object behind it: the native handle and its key collection are both released inside the decode. An animation clip keeps its handle, because XNA has no clip type to become; its duration, target space, two tracks with different bone indexes and different keyframe counts, and every component of every keyframe -- three vectors of different lengths at different offsets -- are asserted, along with out-of-range refusals and the fact that a curve and a clip refuse each other's containers. Two planted bridge defects, a dropped key continuity and an ignored track index, each fail exactly one test |
| CNB model: the bone hierarchy, parts and payloads, materials, meshes, skeleton and lights | cna-ts/extensions/content | CNB's largest schema, read and written through CnbModelData. A reference model with every projected level occupied at once -- two bones in a hierarchy, a part with real vertex and index payloads, a material with distinguishable values in every field and all eight texture slots named differently, a mesh, a skeleton with three matrix sets, a light -- is built with the public API, encoded by CNA's own writer and decoded back. Every assertion is a value: the exact parent links and transforms, the exact payload bytes and lengths, each of the eight texture names through its own slot, the mesh's parent bone and part membership, and all three skeleton matrix sets kept apart by their own diagonals. Refusals are asserted too: another asset type, a corrupted image, a payload that contradicts its declared part (which CNA validates at encode rather than at set, measured rather than assumed), and use after disposal. Two planted bridge defects -- an ignored skeleton matrix-set identity and an ignored texture slot -- each fail exactly one test |
| CNB sound effects, songs and videos: the compiled media schemas | cna-ts/extensions/content | A sound effect round-trips its format, rate, channels, frame count, both loop numbers and its sample bytes exactly, and CNA's own WAV decoder reads a RIFF image into one with the same bytes the header declared; a file that is not a WAV is refused. CreateSoundEffectFromCnb produces a real XNA SoundEffect owning a native audio resource whose name round-trips through CNA, and refuses by name for a format XNA cannot represent -- eight-bit PCM read as sixteen would play as noise -- while leaving the samples readable. Songs and videos carry a stream reference rather than the media, so both schemas are complete and fully testable with no encoded audio or video at all: name, duration and stream reference for a song; dimensions, a non-integer frame rate, duration, soundtrack kind and stream reference for a video. Each of the three refuses the other two's containers. Duration is zero on this artifact because it is built with CNA_AUDIO_PLATFORM=NULL, which is a property of the configuration rather than of the schema; the real 250 ms measurement is made in the browser suite, whose artifact has SDL3 audio |
| CNB SpriteFont: decode a compiled font with its embedded atlas into a drawable SpriteFont | cna-ts/extensions/content | a font CNA encoded decodes with its metrics, its ascending character map and its per-glyph rectangles and kerning; CreateSpriteFontFromCnb builds a SpriteFont whose MeasureString is exact, the copied atlas outlives the font it came from, an unsorted character map is refused at encode time, and an absent fallback character reads as null rather than U+0000 |
| CNB Texture2D: decode a compiled texture and upload it as a real XNA resource | cna-ts/extensions/content | a texture description CNA encoded decodes to the same shape, representation format and level bytes, and CreateTexture2DFromCnb produces a Texture2D whose four readback texels are exactly the RGBA the encoder was given; representation selection reports an unsupported format as absence rather than failure |
| CNB's writers: authoring a container for an asset type CNA has no schema for | CNA-TS | one of every primitive the byte writer offers, decoded back at its exact offset with a DataView -- which is what proves the widths and the little-endian order rather than only that some bytes survived. A u64 survives exactly because it crosses as a bigint; a negative i32 is not read as unsigned; a string carries a four-byte length before its UTF-8 bytes. ToArray copies and Take empties, and the two are distinct. A container built from a custom asset type, custom schema version, metadata, an external reference and a game-defined chunk parses back through the same CnbDocument reader every projected schema uses, with the chunk payload byte for byte. CNB validates structure when a chunk is added -- alignment must be a power of two within its ceiling -- and sizes when the document is built, and both halves are asserted where they happen. Four planted defects all fail: a Take that copies, a u16 written at 32 bits, a dropped power-of-two check, and a limit narrowed but never sent |
| Colour grading through a .cube LUT, as a strip and as a volume | cna-ts/extensions/graphics | the acceptance is a table this package wrote and can apply itself. A size-2 .cube whose transfer is the channel rotation (r,g,b) -> (b,r,g) is parsed, every one of its eight corners is read back and checked against that permutation, and it is then handed to a ColorGradePass twice -- once as the 4x2 strip texture CNA builds from it and once as the 2x2x2 volume -- and run over a sixteen-texel gradient on a windowed OPENGLES3 renderer under Xvfb. The rotation is linear in each channel, so trilinear interpolation reproduces it exactly, and every channel of every texel comes back byte-identical to the rotation computed in JavaScript, through both kinds of table, which also proves no flip, mirror or transpose: the source gradient is asserted to be asymmetric under all three first. Strength is a lerp, so half strength is the exact midpoint and zero strength is the exact source, as is a pass with no table at all. LutSizeForStrip is checked against the shape a strip must have -- N slices of N by N -- for six valid sizes and five invalid ones, which are refused with 0 rather than guessed. Text that is not a .cube file is refused by CNA with its own result rather than parsed into a table that would grade nothing, an index outside the cube is refused rather than clamped, and a non-unit DOMAIN is carried through rather than normalised away. One real binding defect was found by running this: the two getters mint different kinds of handle -- a strip comes back as a borrowed render target and a volume as a Texture3D -- and releasing either through the other's route is refused, which is now what the code does and says |
| Compass and motion sensors: absence, and a real reading through CNA's own injection | cna-ts/extensions/sensors | Absence first, and it is the point of the family: a heading of zero and a rotation rate of zero are both perfectly plausible measurements, so all three sensors report NotSupported with no valid data and refuse CurrentValue rather than returning them. Then the data path, through cna_compass_set_test_backend_ext and cna_motion_set_test_backend_ext -- CNA's own synthetic backends -- which is **injection evidence, not hardware evidence**: no physical compass or motion sensor exists on this host. Every component carries a value no other component has, so a transposed or substituted field is visible: two compass headings that differ by the local declination, a magnetometer vector, a 16-value attitude matrix asserted by first row and diagonal, a quaternion, and three motion vectors with disjoint value sets. Timestamps cross as bigints and are asserted to the tick, which a double would lose. The north-reference flag is installed both ways and read back both ways, so it is proved read rather than defaulted. A backend cannot be swapped underneath a running acquisition. Three planted defects each fail the test: two headings read from one field, a transposed attitude matrix, and gravity read from the device acceleration |
| Compiled Effect creation binding route | CNA-TS + CNA | exact byte payload reaches cna_effect_create_compiled with compiler-verified ABI; three legal conformance-FXB attempts deterministically return the qualified HEADLESS result 6 with invalid output and no shader-execution claim |
| Compute shaders and storage buffers: exact GPU computation | CNA-TS | a dispatch computes values[i] * uScale + uOffset + i over 64 float elements, and all four inputs are separately load-bearing: every element differs, so a dispatch that did nothing leaves the seed, one that ignored either uniform produces a different sequence, and one that ignored the invocation index produces a constant. Two work groups of sixteen touch exactly the first thirty-two elements, which is what proves the dispatch count reaches the GPU. Measured on OPENGLES3 under Xvfb; HEADLESS, SDL_RENDERER and SOFTWARE refuse every create with NOT_SUPPORTED and that refusal is asserted rather than skipped. A typed buffer refuses an element transfer whose element size disagrees with its own |
| Deferred decals: a texture projected onto whatever the prepass drew | CNA-TS | accepted texel for texel, against a prediction that is CNA's own. For every screen texel the test reconstructs the world point the decal shader reconstructs -- the camera is a 90-degree square frustum, so a plane at view depth d spans d world units per unit of device coordinate -- transforms it by the inverse of the box's world matrix and asks cna_decal_pass_is_inside_decal_box whether it lands inside, intersected with the surface the prepass actually rasterised. Five boxes over one surface are each checked against that prediction with an exact texel count and no tolerance at all: the whole rectangle, one half as wide, two small ones in opposite corners that share no texel, and a long box rolled thirty degrees about the view axis that paints a 117-texel diagonal band across the full width -- a shape no axis-aligned box could produce, so the box's rotation demonstrably reaches the projection. A box the surface does not reach paints nothing, and CNA's own box test agrees that it should; a pass with no depth input paints nothing and does not fail. Opacity is checked through the blend rather than through its getter: the pass composites NonPremultiplied, so half opacity over a known clear colour is an arithmetic identity the test computes, including that blend's own alpha rule, and a green tint on a red decal leaves black because the tint multiplies channel by channel. The slope test is semantic: with the prepass normals supplied, a decal projecting the way the surface faces takes none of it and one turned through half a turn takes all of it, and then with the box left exactly where it is, a surface tilted 60 degrees shows, 80 degrees does not, and widening the limit to 85 brings the same picture back -- one number different. Opacity clamps into the unit range, the slope clamps to a right angle and floors at zero, and the tint is taken as given because above one brightens an HDR frame. The input rule is measured rather than left to be discovered: the prepass's own buffers are the depth input and an upload of the same bytes is not, which SetPrepassInputs documents |
| Direct standalone GraphicsDevice construction | CNA-TS | XNA public GraphicsDevice constructor now creates a real second device beside the game one, with no game of its own: cna_graphics_device_create takes an adapter index and presentation parameters and nothing else. A Texture2D made on it round-trips its four exact texels, its viewport and presentation parameters are its own rather than the manager (64x48 against the game 320x240), two caller-created devices live at once each answering with its own profile and size, and a texture from one device is refused by another device SpriteBatch with InvalidOperationException. The ownership rule is asserted directly off the native lifetime rather than inferred from a symptom: a caller-created device releases its own handle exactly once, and disposing a manager-created device leaves the game native lifetime active -- a Dispose that released whatever lifetime it found passed every other assertion and is caught only by that one. The adapter index is the adapter own position in CNA enumeration, which one physical adapter cannot discriminate, so test/standalone-device.test.mjs supplies two deterministic adapters and proves the index and the reported adapter are both the given one |
| Dynamic buffer ContentLost subscription and deterministic release | CNA-TS | the declared events now have a real CNA producer behind them: both subscriptions execute on the qualified artifact, the registration is released with the resource, and disposing twice with a live registration stays harmless |
| DynamicSoundEffectInstance submit, pending buffers and managed refill pump | CNA-TS + CNA | qualified native integration with reentrant/self-removing/throwing callback tests |
| DynamicVertexBuffer and DynamicIndexBuffer creation, typed transfer, readback and IsContentLost query | CNA-TS + CNA | qualified integration covers Discard/NoOverwrite, built-in vertex and 16-bit index round trips, double disposal and parent shutdown |
| Effect and EffectPass ABI-0.7 apply dispatch with native technique/pass reflection identity | CNA-TS + CNA | qualified integration executes cna_effect_apply and cna_effect_pass_apply on effect-owned stock passes; managed/native ownership tests retain the parent, destroy owned views, reject disposed parents and expose no raw handles |
| Extended graphics layer availability probe | cna-ts/extensions/runtime | cna_graphics_ext_is_available answers true on a CNA_CNAEXT=ON build and false on the WEBGL2 artifact, so structural presence is never reported as availability |
| Extended input: raw joysticks and force feedback, on a host with neither attached | cna-ts/extensions/input | cna-ts/extensions/input is a new public subpath, kept out of Microsoft.Xna.Framework.Input deliberately: a joystick's axes are raw, its identity is the platform's GUID, and it may carry hats and trackballs GamePadState has no member for, so folding one into the XNA type would drop data or invent a mapping. What this host proves is the absence contract, and CNA's is consistent and worth pinning -- measured rather than assumed. An identifier no device has is not refused; it is answered *absent*: a disconnected descriptor with zero counts and an empty name and GUID, which is the convention XNA's own GamePad.GetCapabilities follows. The haptic half is the stronger claim -- opening an absent device produces one that reports itself closed, offers no features, and declines InitializeRumble, PlayRumble, StopRumble and SetGain rather than silently accepting a request it cannot perform, which would be worse than an exception because a game would then offer the setting. Disposal closes and releases in that order, once |
| FrameworkDispatcher.Update canonical native pump | CNA-TS + CNA | native lifecycle and dynamic/media pump integration |
| Frustum culling, and the vertex declarations an instancing stream takes | cna-ts/extensions/graphics | the acceptance is two implementations of one predicate, neither derived from the other: CNA's culler and this package's own XNA BoundingFrustum, asked about the same eleven boxes and five spheres built for the same camera. They agree on every one, and the fixture is asserted to be mixed -- neither all visible nor nearly none -- so the agreement cannot be vacuous. The batch routes are checked against the single test rather than against recorded indices: culling a batch keeps exactly what testing them one at a time keeps, in order, and an empty batch keeps nothing. A culler set from one view-projection matrix agrees with one set from two on every case. Culling *transforms* has two traps and both are pinned: the bounds are matched by index and are already world-space, so one shared local box keeps every transform rather than testing them all against it, and a transform with no matching bound is KEPT rather than dropped -- CNA's own header says so, and a caller who miscounted would expect the opposite. Given one world-space bound per transform it answers exactly what culling those bounds answers. The instancing streams are described by CNA rather than written down here: four Vector4 rows on consecutive texture-coordinate channels at sixteen-byte offsets with a stride of sixty-four, and one packed Color for a tint with a stride of four. Five planted binding defects fail; a sixth, replacing the stride route with the constant it returns, survives and is recorded rather than hidden -- a test cannot distinguish a correct constant from the route that produces it |
| Game create, callbacks, one-frame/run/exit/destroy lifecycle | CNA-TS + CNA | qualified ABI-0.7 integration covers seven real game lifetimes and 60/600 frames |
| GamerServicesDispatcher lifetime and Guide state over real CNA gamer services | CNA-TS + CNA | the dispatcher initialises against the running game and reports CNA's own IsInitialized rather than a mirrored flag; a 64-bit platform window handle round-trips as a bigint; Guide's trial-mode simulation, notification position and visibility read and write through CNA. Two XNA rules CNA does not enforce are enforced here and asserted: an un-initialised Update raises InvalidOperationException, and IsTrialMode is the disjunction with SimulateTrialMode. IsScreenSaverEnabled is a platform display property, so with no displays the getter answers true and a write does not take -- reported rather than cached. Nothing fabricates a gamer: SignedInGamers is empty and every screen-showing operation still refuses with GamerServicesNotAvailableException |
| GameWindow stable facade, borrowed handle/state and removable event registrations | CNA-TS + CNA | qualified integration verifies stable Game.Window identity, zero HEADLESS handle, title/state queries, callback registration cleanup and repeated game lifetimes |
| glTF material extensions: clearcoat, sheen, transmission, iridescence and subsurface | cna-ts/extensions/graphics | bound as a handle rather than a value struct because the canonical type holds nine borrowed Texture2D pointers, which do not belong in caller-writable memory -- and it still behaves as a value, which is what is checked: two fresh sets compare equal and hash alike, copying makes an edited set equal to its source and leaves the source alone, and the printed form is {} when neutral and names each feature that is on. The subtle part is what IsNeutral means, and it is measured one field at a time from a fresh set each time: it reports whether a feature *contributes*, not whether a field was written. A sheen colour, a transmission factor, an iridescence factor, a subsurface colour or a clearcoat factor makes the set non-neutral; a sheen roughness, a thickness, a subsurface wrap, a clearcoat roughness or an attenuation distance does not, even though each stores its value and reads back. Every scalar and vector round-trips at float precision with a value no default equals, and the nine texture slots are empty, then filled in three of them and not the other six, then cleared with null. glTF's own non-zero defaults are asserted as such: an iridescence IOR of 1.3, a thickness range of 100..400nm, and a white attenuation colour |
| GPU timers: a measured frame duration | CNA-TS | a non-blocking timer query wrapped around one real frame of drawing and polled on later frames, collecting a sub-millisecond measurement on OPENGLES3 with exactly one sample. A timer creates even where the renderer has no timer query at all, which is CNA reporting the absence through the object rather than by refusing: on HEADLESS it answers IsSupported false and names the missing GL extension in CNA own words |
| GraphicsAdapter: the adapter list, its display modes and its format answers | CNA-TS | The one strict XNA type this package projected structurally and could not fill, because nothing read CNA's adapter capabilities. Its fourteen routes are now bound and the type is real on HEADLESS and on three windowed renderers. The read is deliberately lazy: CNA's adapter routes take a *borrowed* graphics-device handle and permit that borrow only inside a lifecycle callback, so a read at device-creation time is refused with INVALID_STATE -- what is registered there is a source, resolved the first time a caller asks, which for a game is from LoadContent. Asserted: one adapter, its four separate integer identity fields, a current display mode whose aspect ratio matches its own dimensions, a supported-mode list, both XNA profiles with HiDef implying Reach, and a 64x multisample request answered inexactly with a count CNA can actually provide. The description and device name are CNA's canonical XNA values rather than a GPU probe -- the same on HEADLESS and OPENGLES3 -- and that is stated rather than dressed up. A renderer with no adapters still refuses by name; a renderer with no native monitor handle answers zero, which is a legal IntPtr, rather than failing the whole adapter. Three planted defects fail it, including one that only fails because the test compares a case where the back-buffer and render-target queries genuinely disagree: Reach + Single is refused as a back buffer and accepted as a render target |
| GraphicsDevice bound, indexed, instanced and four-codec user draw dispatch | CNA-TS + CNA | 360-symbol bridge dispatch and argument layouts are signature-audited; qualified HEADLESS calls reach CNA; no visible-output claim |
| GraphicsDevice capability query and the compute work-group limits | CNA-TS | XNA had GraphicsProfile, a two-value tier standing in for a capability list; CNA answers per capability and cna-ts asks rather than assumes. All nineteen answers are cross-checked against the renderer own capability bitmask, which reaches the test through a different route, on three windowed renderers whose masks differ (OPENGLES3 0x7ffff, SOFTWARE 0x1af7, SDL_RENDERER 0x1000) and on HEADLESS, which answers ten of nineteen. That the bitmask form is one shifted by the ordinal form is a compile-time fact rather than an assumption: tools/cna-abi/contract.json asserts it for all nineteen |
| GraphicsDevice copied state, scalar state, sampler/texture, buffer and render-target binding routes | CNA-TS + CNA | qualified ABI-0.7 integration verifies successful assignment, null/unbind, stable read-after-write facade identity and render-target state; managed suite covers bounds, disposed, duplicate, wrong-device and rollback validation |
| GraphicsDevice status query | CNA-TS + CNA | qualified HEADLESS device reports the canonical Normal status through cna_graphics_device_get_status |
| GraphicsDevice.Clear and Present | CNA-TS + CNA | 60/600-frame qualified native integration |
| GraphicsDeviceManager create/configure/apply and callback-scoped device borrowing | CNA-TS + CNA | qualified native integration and ownership tests |
| Keyboard, Mouse, GamePad and Touch polling routes | CNA-TS + CNA | qualified HEADLESS native integration; physical device behavior is separate |
| Level-of-detail groups, and the hysteresis that damps a boundary | CNA-TS | levels added out of order come back sorted, and every band is checked on both sides of every threshold -- a threshold ends its level, and past the last one the answer is -1 rather than a clamp to the coarsest. The hysteresis is the part worth proving: with a margin of 3 around a boundary at 25, climbing holds the near level until 28 and descending holds the far one until 22, and the two walks are asserted not to be each other reversed, which is what separates hysteresis from rounding. A jump of more than one level is deliberately undamped, and that is asserted too. Screen-space selection projects radius * (height / 2) / (distance * tan(fov / 2)) exactly, and halving with distance. Three planted defects fail: a Reset that does not forget, a margin that never reaches CNA, and a projection that ignores its distance. CNA associates each level with a ModelMeshPart and can hand it back; that half is deliberately unprojected because this package ModelMeshPart is managed and has no native handle to give, so the route could only ever answer nothing |
| Light probe baking: six cube faces rendered per probe, and the probe they produce | CNA-TS | accepted on real captures on a windowed OPENGLES3 renderer under Xvfb. Which face the scene callback is being asked to draw is decided inside the callback by matching the view CNA handed over against what FaceView reports for that face and that capture point, so the callback's own behaviour is the evidence that each face got its own camera -- and a bake that gave one camera to every face, or the six in another order, cannot produce the list the test asserts. Every face is captured through the square 90-degree frustum the test builds itself. Then the acceptance: light exactly one face and the probe is brightest looking that way and a hundredfold darker looking the other, light the opposite face and the two answers swap, light a third and the brightest direction moves with it. Painting the same face at half a byte scales every one of the nine coefficients by that fraction, which is the projection's linearity in radiance measured rather than assumed; an unlit scene bakes a probe that is zero in every direction; a fully lit one bakes an isotropic probe whose direct term is twenty times any other. A two-cell volume is baked with the scene lit only for the cell the callback recognises, and only that cell carries light, so each probe was captured from its own place; the same bake from a point away from the origin produces the same probe. Visibility is a distance rather than a colour: painting every face one known byte records exactly that fraction of the far plane, with the mean squared equal to the mean squared because a uniform face has no variance, and a light bake afterwards keeps every distance. An exception thrown by the scene comes back out of the bake as itself and the device still bakes afterwards, which is what says CNA was not left holding a bound target. HEADLESS cannot read a target back, so its baker reports unsupported, every bake refuses by state, and the scene callback is never called -- asserted rather than skipped, because a bake that drew and threw the result away would look identical from outside |
| Light probes as values: nine coefficients, six occluder distances, and the grid over them | CNA-TS | checked against arithmetic with every constant measured from CNA rather than written down. A probe's irradiance is a fixed quadratic in the surface normal with five constants, and the test lights one coefficient at a time to read each of them out of CNA -- twice over for the two that appear in more than one term, which have to agree -- then predicts what a probe with all nine distinct coefficients answers for nine normals, and it does, to 1e-4 on every channel. A direct-only probe is identical in every direction, an overshooting fit comes back at zero because negative light is not a look, a degenerate normal is treated as straight up, and scaling multiplies the irradiance with the coefficients. Equality is by content and content includes the position, which the volume's own behaviour then shows: a probe copied into a cell takes the cell's position, so it stops being equal to its source until the source is moved to match. The grid's probe positions are an even step along each axis computed by the test, an axis holding one probe puts it at the box's minimum, and trilinear sampling is exact at a cell, at a half and at a quarter, with a position outside the box clamped in rather than refused. Visibility floors both distances at zero and the mean squared at the mean squared -- no distribution has negative variance -- and the weight is Chebyshev's bound, computed by the test from the numbers it stored: one when nothing is known to be in the way, and a diagonal between two recorded axes mixing both rather than picking either, which is a third number from either alone. Twenty-two planted binding defects fail and none survives |
| MediaPlayer source/song creation, queue controls, position and visualization | CNA-TS + CNA | generated legal silent WAV under qualified NULL-audio integration |
| Model.Draw effect/pass/indexed-draw pipeline | CNA-TS + CNA | qualified HEADLESS Model XNB executes buffer/index binding, BasicEffect matrices, real EffectPass.Apply and DrawIndexedPrimitives without a special native model renderer |
| Modern CNA device layer: host CPU/memory, power, display scale and safe area, locales, clipboard, cameras | cna-ts/extensions/devices | the layer's availability is asked before any reader is offered, CNA's logical core count matches Node's independently, an absent battery charge reads as null rather than zero so a low-charge comparison cannot misfire, a windowless session's zero content scale and empty safe area are recorded as CNA's own answers, locales come back as language/country pairs, the clipboard reports acceptance rather than throwing where there is none, and camera support is kept distinct from camera count |
| Modern CNA PBR material and render-pipeline value defaults | cna-ts/extensions/graphics | cna_pbr_material_init and cna_render_pipeline_settings_init are pure value operations CNA documents as answering in either build, and they do on the qualified artifact; the facade seeds its value objects from them rather than from numbers written in TypeScript |
| Modern CNA platform identity, renderer selection and runtime log | cna-ts/extensions/runtime | nine native integration assertions over 37 handle-free routes, including the pre-latch and non-desktop refusals CNA reports as state |
| Modern CNA post-process chain: bloom, tonemapping, FXAA, SSAO, screen-space reflections | cna-ts/extensions/graphics | every pass property round-trips through CNA at float precision, CNA's own quality tiers rise with quality and its roughness-blur clamp is recorded rather than avoided, each pass reports its own name and its truthful IsSupportedOn answer on the HEADLESS renderer, a chain applies over real render targets, and GPU timing reports what the renderer actually gave rather than what was asked for |
| Modern CNA render pipeline object lifetime, resize, frame and statistics | cna-ts/extensions/graphics | on a CNA_CNAEXT=ON artifact a pipeline is created, resized, begun, ended, queried for statistics and disposed twice safely; where the layer is compiled out construction reports CNA_RESULT_NOT_SUPPORTED and the native suite asserts that branch instead |
| OcclusionQuery construction, ordering, reuse, completion/result dispatch and disposal | CNA-TS + CNA | qualified integration executes the real query state machine without fabricating PixelCount; managed suite covers completion and exact result identity |
| RenderTarget ContentLost subscription and deterministic release | CNA-TS | subscription executes on the qualified artifact and its registration is released before the target is, ordered after the bound-target guard so a refused disposal leaves the subscription in place |
| RenderTarget2D/Cube construction, metadata, binding and inherited exact transfer routes | CNA-TS + CNA | qualified HEADLESS integration verifies RenderTarget2D and RenderTargetCube creation, metadata, 2D/cube-face bind/unbind identity, bound-destroy rejection and parent shutdown; managed backend covers cube metadata, face identity and duplicate validation |
| Shadow-map maths: what a quality tier costs, and where a light looks from | CNA-TS | the quality ladder is asserted both as its exact five values (512/512/1024/2048/4096 and filter radii 0/0/1/2/2) and as a property -- monotonic, and Ultra strictly larger than Disabled -- so a renumbering that kept the same values in a different order still fails. The geometry is checked as geometry rather than against numbers copied out of a run: the light view's rotation is orthonormal, three unit rows mutually perpendicular, and all eight corners of the scene box transformed through view times projection with XNA's own Matrix.Multiply and Vector3.Transform land inside the light's clip volume. Fitted rather than merely contained: the corners reach at least 0.95 of the frustum on both screen axes, measured at 0.998 for a large scene and 0.980 for a unit box, and the depth range is exactly zero to one. A different light direction produces a different view. Three planted defects fail: a quality that never reaches the size lookup, a projection computed from the identity, and a light that always points down. The shadow map object's own state -- size, filter radius and quality read back from its tier, and a depth bias that round-trips -- is verified on HEADLESS at 2048px |
| SoundEffect PCM creation, instances, controls, Apply3D and disposal | CNA-TS + CNA | qualified NULL-audio integration; no audibility claim |
| SpriteBatch create, Begin, Draw/DrawString submission, End and destroy | CNA-TS + CNA | qualified native integration and template 60/600 frames |
| SpriteBatch Effect-bearing Begin | CNA-TS + CNA | qualified HEADLESS integration executes cna_sprite_batch_begin_with_effect with a real BasicEffect; managed lifetime lease, device checks, null behavior, disposal guard and native-failure rollback are covered |
| SpriteBatch explicit render-state and transform Begin | CNA-TS + CNA | qualified integration executes copied Blend/Sampler/DepthStencil/Rasterizer descriptors; managed tests verify identity, wrong-device checks and post-success immutability |
| StorageDevice selector, StorageContainer and stream CRUD | CNA-TS + CNA | qualified native integration in isolated XDG storage |
| Texture2D create, Color transfer, regions/mips, FromStream and PNG encoding | CNA-TS + CNA | qualified native integration with deterministic pixel checks |
| The .cnj compile front end, and the dependency lists a build system needs | CNA-TS | a .cnj document authored here from its own specification -- so what the runtime decodes is evidence about the compiler rather than CNA agreeing with itself -- compiles into a .cnb the runtime package reads back with every key's position, value and continuity exactly as the JSON declared, and with the loop kinds it named. The content name defaults to the document stem and honours an override. A document naming a PNG sidecar reports both files in AbsorbedFiles, which is the dependency set a build system watches, and the four texels the PNG carried arrive through the compiler unchanged. An unsupported asset type, a missing sidecar and a missing document are each refused with CNA's own IO result rather than a message match, which is what distinguishes CNA refusing from this package refusing first. It lives in cna-ts-content because it takes filesystem paths |
| The atmosphere's model, its sampling sequence, and the two mappings around a cube map | CNA-TS | everything about the atmosphere that is arithmetic rather than rendering, checked on relationships and identities rather than on recorded numbers. A sun below the horizon leaves a sky twenty times darker than one above it; the horizon is brighter than the zenith because a horizontal ray passes through more air; a clear zenith is blue then green then red, which is Rayleigh's fourth-power wavelength dependence; and aerosol brightens the sky and washes the colour out of it, because Mie's has none. A view direction with no length is treated as straight up, and a direction is a direction whatever its length. The roughness ramp is a straight line between mip zero and the last, inverse in both directions, answering rather than refusing for a chain with nothing to index and clamping a roughness outside the unit range. The Hammersley sequence is checked point by point against its own definition -- evenly spaced along one coordinate, the radical inverse of the index in base two along the other, and no two points sharing it. GGX importance sampling at roughness zero is the normal itself, roughening it spreads the sample further, and every sample is a unit direction inside its normal's hemisphere. The six cube faces look down the six axes; the panorama mapping puts the poles at nought and one, every horizon direction halfway down, opposite axes half a turn apart and perpendicular ones a quarter -- and the two routes are made to agree by sending one's answers into the other. The view ray a screen point looks along is checked as geometry too: the centre looks the way the camera does, the edges are forty-five degrees off a ninety-degree frustum, and a yaw rotates every ray about the up axis by exactly that rotation, which is asserted as a rotation rather than as an angle because a tilted ray sweeps through less than the yaw. The three setters that guard three different ways are kept apart and asserted apart: turbidity clamps into the model's range, a sky's negative intensity keeps the previous value while a skybox's is floored at zero, and a sun direction is normalised with a degenerate one a silent no-op |
| The canonical PBR material as a value: equality, hashing, printing and device state | cna-ts/extensions/graphics | CNA carries the physically-based material in two C shapes -- the layout frozen before the KHR_materials_* factors existed, and the canonical one every accessor on the C++ type corresponds to -- and this projects the canonical one. It is a value and is checked as one: two materials built the same way compare equal and hash alike, one edited field makes them differ and changes the hash, the same edit twice gives the same material again, and two materials never share their per-slot arrays. CNA's own defaults are glTF's -- fully metallic and fully rough, an IOR of 1.5, an opaque white base colour, every slot on UV channel zero with a neutral transform -- and each is asserted against what glTF specifies rather than against a number recorded from a run. The printed form names the fields it carries, including all four channels of a base colour no default equals. What ApplyState does is qualified by reading CNA's device state back rather than by trusting the wrapper's, which cannot see it: a blended material leaves SourceAlpha/InverseSourceAlpha, an opaque or masked one leaves One/Zero, and a double-sided one leaves CullNone -- each predicted from the XNA state the C++ names, not from a measurement. Fifteen planted binding defects fail and none survives |
| The cascaded, spot and cube shadow passes, beside the flat one | CNA-TS | each of the three checked against the pure maths beside it, which touches no pass at all. A cascade's split distances are the ones ComputeCascadeSplitDistances returns for the same camera and lambda, twice over with two lambdas that give different answers -- and a lambda of zero divides the range evenly, which the test writes down as a closed form. The splits stay where the camera put them when the light moves, while every cascade transform moves with it, which is the separation the update is for. A spot map's light view-projection is ComputeSpotLightView times ComputeSpotLightProjection for the same light, multiplied by the test itself, and it carries real translation terms rather than being an identity that would agree with anything. A cube's face size is the cube route's answer for its tier and not the flat route's. Snapping is a pure function checked against its own definition: every axis lands on a whole number of the cascade's texels, never moves further than one, and the axis the light looks along is untouched. On a windowed OPENGLES3 renderer under Xvfb the atlas is surveyed slice by slice at six steps, so what is asserted is the change at each one: a fresh atlas is untouched storage rather than a cleared one, the first pass clears every cascade, a cast into cascade zero changes that slice alone, a later cast into the last changes only that one and leaves the first exactly as it was -- which is what says a Begin clears its own viewport rather than the atlas. A spot pass renders a floor and then the same floor raised towards the light: nearer records a smaller depth and covers more of the cone. All six cube faces open and close. HEADLESS compiles no caster program, so all three still allocate their storage and accept the passes they cannot fill while refusing their effects by name -- except the cube, whose faces are larger than that renderer's back buffer, which it says in a message carried by CNA_RESULT_INTERNAL. Twenty-two planted binding defects were run and twenty-one fail; the survivor is recorded rather than hidden, because upstream finding 16 is exactly why it cannot fail |
| The depth/normal prepass: linear depth and view-space normals, rasterised and read back | CNA-TS | accepted against CNA's own rasteriser on a windowed OPENGLES3 renderer under Xvfb. One flat quad is drawn twice in the same frame into render targets of the same size, read back by the same routine: once through a stock BasicEffect, which shares no code with the prepass, and once through the prepass itself. The two rectangles agree to within 1.5 texels on every edge, so this renderer's screen and readback conventions cancel between them and what is left is whether the prepass puts the scene where the renderer puts it. The depth is exact rather than merely below the far plane: a quad at view depth 10 with a far plane of 100 reads back as the number CNA's own pack and unpack routes give for 0.1 through an eight-bit target, to five decimals, and the whole buffer holds exactly two values -- that surface and the far-plane clear -- with the surface count equal to the covered texel count. The normals are exact too: every covered texel carries (128, 128, 255), which is +Z in view space encoded, with the alpha that marks a texel as carrying no velocity. An untouched normal texel carries the depth buffer's white, because one clear serves a bound target set. Roughness round-trips and clamps into the unit range at both ends; the pass count agrees with the multiple-render-target answer; velocity is off and its texture is null rather than an empty buffer; and both prepass programs are lent and are real. Two findings came out of it: the packed encoding delivers one part in 255 rather than the documented one part in 2^24 once it is stored in the eight-bit target it is written for, which the test measures with a sweep and a 256-level control (upstream finding 13), and three of its ordering refusals answer INTERNAL where the header documents INVALID_STATE (upstream finding 14). Both are asserted as they behave, so a repair fails and says so |
| The glTF material bridge, and the one conversion it performs | cna-ts/extensions/graphics | the bridge is the only thing in this family that converts rather than carries: a glTF base-colour factor is a linear float vector and a CNA albedo is an eight-bit colour. That conversion is measured over a table of seven inputs rather than from one point, and every one matches round(x * 255) -- (1, 0.5, 0.25, 0.5) becomes (255, 128, 64, 128). Everything else is asserted to arrive unchanged, each factor set to a value no default equals, and the per-slot values are asserted to arrive in their own slot and nowhere else: one coordinate set written to the emissive slot leaves the other six at zero, and one transform written to the metallic-roughness slot leaves the other six neutral. The extension bridge is checked the same way against a fresh extension set. glTF's own defaults come from CNA rather than from numbers written here, so a change upstream is a diagnostic rather than a silent difference |
| The Guide's two asynchronous screens, and CNA's own answers to them | CNA-TS | Guide.BeginShowMessageBox and Guide.BeginShowKeyboardInput are the only genuinely asynchronous begin/end pairs in XNA, and both now work. A message box does not complete inside the Begin call and says so, carries the caller's async state untouched, makes Guide.IsVisible true, and holds the focus button XNA was given rather than a default. Answering it runs the continuation exactly once with the very result object Begin returned -- the IAsyncResult contract -- and EndShowMessageBox returns the button that was pressed; a second box with a different press returns that one instead, so the answer is not a constant. The keyboard input reports its title, description and starting text back from CNA, and a cancel makes EndShowKeyboardInput return null rather than an empty string, which is XNA's distinction. A second screen while one is pending is INVALID_STATE, and End before the answer is an error rather than a wait. Three planted defects fail: a dropped focus button, an End that accepts a foreign or incomplete result, and a continuation that fires twice. Nothing here fabricates a gamer, a sign-in or a peer: CNA draws the screen and answers it, which is the same deterministic injection the sensors and the camera use |
| The particle draw: live particles painted where the camera puts them | CNA-TS | accepted on the texels the draw painted into a render target on a windowed OPENGLES3 renderer under Xvfb. The scene is made predictable rather than guessed at: the simulation is pinned to the CPU, every source of variance is zero and the emitter's speed is zero, so all 32 particles stand exactly on the emitter -- which the pool, read back, confirms is the one point and no other. Two systems at different world positions with different particle sizes are drawn through one camera the test built itself from CreateLookAt and CreateOrthographic, and each painted region is checked against where that camera puts that emitter and how wide that particle size is there: both squares land within 1.5 texels on every edge, cover their predicted area to within 20%, and are painted in the particle texture's colour and no other. Drawing either system alone puts its square in the same place, a system with an emission rate of zero paints nothing at all and does not fail, and moving the camera two world units slides both squares by exactly what the new view predicts. CNA's default capacity comes from the route that takes no capacity and is 1024, which tools/cna-abi/contract.json pins to CNA's own header with a _Static_assert, and the storage-buffer binding point the API states is the one CNA's own particle GLSL declares. Nine planted binding defects fail, including a draw that swaps the view and the projection, one that sends the view as both, one that drops the texture, a settings setter that loses the particle size, and a softness setter that never reaches CNA. One survives and is recorded as unkillable: creating at the default capacity by asking for 1024 explicitly is indistinguishable from asking CNA, because no route says which was used. The soft-particle fade is the one thing not accepted -- the depth image and the softness reach CNA and change nothing that is drawn, which is upstream finding 12, asserted as it currently behaves so a repair is noticed |
| The particle simulation, and the pure functions behind it | CNA-TS | cna_particle_system_step integrates one particle with no system, device or GPU, and cna_particle_system_random is the deterministic generator the emitter draws from -- so the simulation is checked against arithmetic rather than against numbers recorded from a run. Gravity of -10 over half a second gives velocity -5 and position -2.5 exactly, an axis with no gravity is unchanged, and the age advances by the step. Two half steps and one whole step both reach velocity -10 but land in different places, which is what shows the elapsed time reaching a stepwise integrator rather than being ignored; a zero step changes nothing; and Step returns a new particle rather than mutating the caller's. The same seed always draws the same value and different seeds draw different ones. The system itself round-trips its emitter settings through a different route than they went in, reports its capacity, and says whether it is simulating on the GPU and why not -- forced onto the CPU here, which is how a game gets the same answer on every renderer. Three planted defects fail: a fixed step, a seed that never reaches the generator, and gravity that is always the default |
| The physically-based effects, skinned and not, and the material they carry | cna-ts/extensions/graphics | the acceptance is two independent paths into one state. A whole material goes on with one route, and every field is then read back through the per-field accessors -- routes the apply never touched -- before the extractor is asked for the material again and CNA's own equality compares it with what went on. The per-field setters then move the same state, which is what makes those two paths two rather than one. A fresh effect starts at exactly the material's own defaults, which is what makes them one type. Per-slot state is asserted to land in its own slot: a coordinate set and a transform written to the normal slot arrive there and the base-colour slot keeps its own, and an sRGB flag cleared on one slot leaves the others set. The skinned effect carries the same material and its own skeleton: four bone weights per vertex by default, a translation and a scale that survive the round trip, identity that stays identity, and a request for fewer bones than were set answering with that many. On a windowed renderer both are real compiled effects with named techniques whose passes execute, which HEADLESS constructs and refuses. The texture slots are upstream finding 19 and are asserted as measured rather than as documented |
| The rest of the post-process passes: tonemapping, depth of field, lens flare, motion blur, chromatic aberration and film grain | cna-ts/extensions/graphics | every pass is checked against something that is not the pass. The tonemapper is the clearest case: CNA ships the curve twice, once as the shader and once as cna_tonemap_pass_tonemap_channel on the CPU, and the headless suite first pins that CPU route to the published closed forms -- Reinhard is exactly v/(1+v), ACES is the Narkowicz fit, None is a clamp not a curve, exposure multiplies before the curve and gamma raises after it -- and the windowed suite then predicts every channel of every texel of a drawn frame through it, at five different settings that produce five different frames. Bloom's extraction is a soft knee, not the subtraction its comment used to claim: measured at (0.8, 0.8) it returns 0.2, a quarter of the channel, and the headless test now predicts the whole ramp from the knee's shape and checks monotonicity in both value and threshold. Depth of field's circle of confusion is checked against the thin-lens equation written out in the test, at five lens settings: it is zero at the focus distance, symmetric about it, and opening two stops doubles it exactly. Each pass's own off switch is an exact identity on a real GPU -- a bloom of no intensity, a bloom whose threshold is above every channel, a flare of either, a blur of no strength, an aberration of no strength, grain of no intensity, and the layer's own blit -- eight of them, all byte-exact. Turned on, each must change the frame in the way its name means: a bloom only ever adds light, chromatic aberration moves red and blue and leaves green untouched at every texel, grain changes most of the frame by amounts that differ per texel rather than a constant offset, and FXAA leaves the smooth interior alone and touches the border it clamps at. The GLSL the layer hands out is never compared against a dialect: what is asserted is that each source is a real program, that the packed-depth SSAO variant is a different program from the plain one, and that the iridescence source is the function library it claims to be |
| The retro effects: a CRT shader, an ASCII quantiser and a depth-quantising effect | cna-ts/extensions/graphics | the CRT is drawn through the layer's own fullscreen pass over a flat grey source, so what comes back is the pattern the shader adds and nothing else. With all four parameters at zero it is a byte-exact copy, which is what makes each later change attributable. Scanlines darken alternate rows by exactly the intensity -- 200 becomes 100 at 0.5 and 150 at 0.25, on every texel of every second row, with the rows between untouched -- and a vignette is verified as radial rather than as recorded numbers: symmetric under both mirrors at all 64 texels, strictly brightening along the diagonal towards the centre, a corner below half the source and a centre above nine tenths of it. Every CRT parameter is written with a value that differs from the default it replaces, so a setter that did nothing is caught rather than agreeing with itself. The ASCII effect's grid is the source size over the cell size and never the destination's: a 32x24 source drawn into a rectangle four times its area gives 4x2 in 8x12 cells, 8x6 in 4x4 and 2x3 in 16x8, each checked against that division, and 0x0 before anything has been quantised. An AsciiPass lends its own effect, and writing a cell size through the borrow changes the pass's own. The depth effect is qualified as native state rather than pixels and says so: it quantises a depth input a colour blit does not supply, so its mode and dither round-trip through CNA and no picture is claimed |
| The shadow depth pass: casters rendered into a shadow map, and the depths read back | CNA-TS | accepted on the depth texels a real pass produced on a windowed OPENGLES3 renderer under Xvfb, against a prediction the test computes rather than a measurement copied out of a run. The pass transform is cross-checked first: the light view-projection CNA reports for an open pass equals the product of ComputeLightView and ComputeLightProjection -- two pure routes that take the same light and scene box and touch no shadow map -- multiplied in the test itself, so three CNA routes and one local identity have to agree. Then the geometry: one asymmetric quad inside an asymmetric scene box, projected corner by corner through that matrix into texels and depth, and every prediction checked against the rendering. An empty pass leaves all 262144 texels at exactly 1.0; the occluder darkens exactly the predicted rectangle to within 1.5 texels on each edge and fills 98% of its area; the recorded depth is the projected depth to within 1e-5; two heights eight units apart differ by exactly the projection's depth scale times eight and cover identical texels; and the same draw outside Begin/End reaches none of the map, which is what shows End unbinding it. The two lent caster programs are told apart by what they rasterise -- the skinned one writes nothing from vertices carrying no bone weights -- because their handles cannot tell them apart: CNA hands out a fresh handle for every borrow. Borrows are counted: the map refuses destruction while one is outstanding, so each is taken once and returned before the map. Fifteen planted binding defects fail, including a dropped scene box, a negated light direction, an End that reports success without closing the pass, either caster getter wired to the other route, a depth texture re-borrowed on every read, and a Dispose that destroys the map before returning its borrows |
| The volumetric and atmospheric screen-space passes: aerial perspective, height fog, light shafts and volumetric fog | cna-ts/extensions/graphics | three of the four publish the arithmetic their shader runs as a pure C route, and that is where the acceptance is. Air mass is checked against Kasten-Young written out from the published formula at ten directions and distances -- one atmosphere straight up, about thirty-eight along the horizon, linear in distance until that ceiling, clamped at zero for a negative distance, and a zero-length direction treated as straight up rather than divided by. Transmittance is checked against Rayleigh per channel plus a grey Mie term at seven turbidities and air masses: no air takes nothing, blue is scattered hardest, and more aerosol takes more light while narrowing the spread between channels, which is why haze washes colour out rather than tinting it. Height fog's optical depth is the closed form of the integral the shader marches, checked at ten configurations and in all three of its branches: no fog at all, a level ray that accumulates linearly, and a climbing ray that converges while a descending one runs away. Moving the layer up with the camera puts the answer back exactly. On a windowed renderer each pass's own off setting is a byte-exact copy -- five of them -- and turned on, the fog moves every texel monotonically towards its own colour and never past it, a descending camera gathers more than a level one at the same density, and enough density gives exactly the fog colour. Eight planted binding defects fail and none survives |
| TitleContainer.OpenStream and default ContentManager title-storage acquisition | CNA-TS + CNA | qualified integration reads package.json through the CNA title-storage count/copy route; CNA-TS rejects absolute/traversal host paths and default ContentManager appends RootDirectory plus .xnb |
| Typed text, IME composition and the mouse cursor | cna-ts/extensions/input | The one input family CNA pushes rather than letting a game poll, because composition is: an IME sends editing updates and candidate lists between the keystroke and the committed character, none of which fits a per-frame snapshot. XNA had no concept of any of it -- Keyboard.GetState reports physical keys, a different question from what the user typed the moment the layout is not US English. Driven through CNA's own _ext injection hooks, which is injection evidence rather than hardware evidence and says so. Nothing assumes ASCII: 'a', 'e-acute', two CJK ideographs and U+1F600 are delivered as six UTF-16 code units -- the emoji as the surrogate pair the platform actually sends -- and appending them rebuilds the text exactly; five characters from six code units. Composition text round-trips as UTF-8 with its selection start and length kept apart, and candidate lists keep their order, selection and orientation. A disposed subscription receives nothing further, and an exception thrown out of a handler is contained at the boundary rather than unwound into compiled C, with a later subscriber still running. Start, Stop, the typed start and the input rectangle all succeed while IsActive stays false, because HEADLESS has no window to start input on -- measured, not assumed. A cursor is an owned handle that closes and releases in that order and refuses after disposal. Three planted defects each fail the test: a code unit truncated to a byte, a composition Length reported as its Start, and an unsubscribe that never reaches CNA |
| VertexBuffer windowed upload with Discard or NoOverwrite | CNA-TS | ABI 0.16 added cna_vertex_buffer_set_data_raw_at_with_options; the adapter routes non-None options through it and keeps the plain route for None |
| VertexBuffer/IndexBuffer mapped public value transfer | CNA-TS + CNA | qualified integration verifies four exact built-in vertex codecs and 16-bit index transfer; no JavaScript object serialization or layout guessing is used |
| VertexDeclaration, VertexBuffer and IndexBuffer resource construction/readback used by Model XNB | CNA-TS + CNA | qualified native Model reader integration |
| VideoPlayer construction and control/cached-property state without decoded frames | CNA-TS + CNA | qualified native control-state integration |
| VideoPlayer.GetTexture borrowed frame projection and its decode identity | CNA-TS | cna_video_player_get_frame_ext's monotonic decode generation is what made a safe projection possible: GetTexture now hands back a non-owning Texture2D over the player's own frame, returns the same object while the generation is unchanged, and retires it the moment anything else touches the player -- the exact window CNA says the handle is valid for. Before any decode the identity reads absent with generation zero and a negative presentation time, reading it twice does not advance the count, and neither path fabricates a texture. Frame progression across a real decode stays fixture-pending |
| Windowed Linux qualification: real GPU pixels through the public XNA drawing path | CNA-TS | against an OPENGLES3/SDL3 CNA library under Xvfb, a RenderTarget2D cleared to (12, 34, 56, 255) through GraphicsDevice.Clear reads back all sixteen texels exactly, the renderer reports itself as OPENGLES3 with a 16384-texel limit and 0x7ffff capability flags, a stock BasicEffect pass applies for real rather than returning the HEADLESS not-supported branch, and 60 and 600 SpriteBatch frames run without drift. Opt-in through CNA_WINDOWED_LIBRARY, and it skips with a reason where no windowed library or display exists |
| Windowed qualification across three renderers, with GameWindow and adapter state | CNA-TS | The windowed suite now runs against every renderer it is pointed at and has been run against three: OPENGLES3, SDL_RENDERER and SOFTWARE, all ABI 0.21 and all under Xvfb. Each reports its own identity, capability flags and texture limit; each applies a stock BasicEffect for real; each runs 60 and 600 frames. GameWindow is exercised where HEADLESS cannot reach it: a title written and read back and restored, a native handle that stays a bigint, a screen device name, an orientation, and AllowUserResizing flipped and restored. Client bounds are measured rather than assumed, because the renderers disagree honestly -- OPENGLES3 and SDL_RENDERER report the 320x240 the manager requested, SOFTWARE reports 0x0 because it presents through a surface -- so what is asserted is that a renderer reporting a client area reports the requested one. SDL_RENDERER and SOFTWARE produce the exact sixteen render-target texels; OPENGLES3 no longer does, and that regression is asserted as measured rather than skipped, so its repair fails the test. See docs/upstream-cna-findings.md item 7 |

## VERIFIED_WEBASSEMBLY

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Browser GamePad: connection, capabilities, buttons, thumbsticks and triggers | CNA-TS | test/wasm-browser-input.mjs drives GamePad.GetState/GetCapabilities from inside an ordinary XNA Update in headless Chromium. With no controller attached all four PlayerIndex slots report IsConnected false rather than an invented device; with a Gamepad API device emulated at the browser boundary -- the integration point SDL3's Emscripten joystick driver actually reads -- the capability table, A and D-pad button states, both thumbstick axes with CNA's Y-inversion, the analog left trigger and an advancing PacketNumber all arrive, and releasing returns every one of them to zero. SetVibration reports false, because the browser's standard mapping exposes no rumble to SDL. Three planted defects -- swapped thumbsticks, an always-connected capability table and a previous-position that reports the current one -- each fail exactly one of these tests |
| Browser off-screen rendering with exact pixel readback | CNA-TS | a RenderTarget2D is created, bound, cleared to an exact colour and read back through Texture2D.GetData in headless Chromium on WebGL2; all sixteen texels equal the cleared value, and disposing a still-bound target is refused |
| Browser sound effects: PCM construction, exact duration and the instance state machine | CNA-TS | a quarter second of 16-bit mono PCM built in the page reports exactly 250 ms and 2,500,000 ticks, volume, pitch, pan and looping round-trip through CNA at float precision, and an instance walks stopped -> playing -> paused -> playing -> stopped. Audibility is deliberately not claimed: a browser will not start a WebAudio context without a user gesture, and SoundEffect.Play reports whether the runtime accepted the sample rather than whether anyone heard it |
| Browser title storage and ContentManager XNB loading | CNA-TS | the browser harness writes assets into the module filesystem, reads exact title bytes back through TitleContainer.OpenStream, and loads an uncompressed Texture2D XNB and an LZX-compressed SpriteFont XNB through an ordinary ContentManager; a missing asset refuses with CNA_RESULT_IO rather than reading as empty |
| Browser TouchPanel: the touch collection and XNA's press/move/release states | CNA-TS | test/wasm-browser-input.mjs dispatches real Chromium touch events and reads TouchPanel.GetState from inside Update. Before the first finger there is no touch device at all, which is distinct from a device with nothing on it. A press arrives as TouchLocationState.Pressed with no previous location, the next frame is Moved at the same point, a move keeps the identifier and reports the press position as its previous one, a second finger is Pressed while the first stays Moved, and lifting reports Released exactly once before the collection empties. The XNA enumerator visits the same identifiers in the same order as the indexer |
| Browser/Wasm CNA runtime | CNA-TS | 60 and 600 real frames of the public XNA Game/GraphicsDeviceManager/Texture2D/SpriteBatch path in headless Chromium on a WebGL2 context, ABI 0.21.0, no uncaught page error |
| CNB curves and animation clips in a browser | cna-ts/extensions/content | the same two schemas over the WebAssembly backend, and the one that settles a wasm32 convention with evidence: cna_curve_key_collection_add takes CNA_CurveKey **by value**, and cna_cnb_encode_animation_clip takes a descriptor holding a pointer to an array of descriptors each holding a pointer to an array of 48-byte keyframes. Both cross correctly -- the page's decoded curve evaluates identically to the one it authored at ten sample positions, and every keyframe component of both clip tracks comes back exactly. All the layouts involved are measured by the Emscripten probe rather than written |
| CNB in a browser: the same compiled-content API, on the WebAssembly backend | cna-ts/extensions/content | a page encodes a texture with CNA's own writer, parses the container back and uploads it with CreateTexture2DFromCnb, and the four readback texels are exactly the RGBA the encoder was given -- the same assertion the Node suite makes, through the same public API, against a different backend. The container primitives agree across backends too: the CRC-32C check value and the magic are the same C routines either side |
| CNB media in a browser, including a measured sound-effect duration | cna-ts/extensions/content | the same three schemas over the WebAssembly backend. The browser artifact is built with SDL3 audio rather than NULL, so this is where the sound effect's duration is actually measurable: a quarter second of 8 kHz mono PCM16 encoded to .cnb, decoded back and turned into an XNA SoundEffect reports exactly 250 ms and 2,500,000 ticks, which is arithmetic on the frame count and therefore exact evidence that the samples and the rate both survived. Both loop numbers, the sample bytes, and the song and video descriptions are asserted as on Node |
| CNB model in a browser: the same schema, on the WebAssembly backend | cna-ts/extensions/content | a page builds a model with CnbModelData, encodes it with CNA's own writer and decodes it back, asserting the same values the Node suite asserts: both bone parents and both transforms, the part's stride and its exact vertex and index payloads through wasm32 memory, two named texture slots beside a third left empty, the mesh's parent bone and part list, all three skeleton matrix sets kept apart by their own diagonals, and the baked light. The structure layouts are measured by the same Emscripten probe as the rest, never hand-written. A planted defect that ignored the skeleton matrix-set identity fails this test; a second, which returned zero for every bone parent, passed until the test was strengthened to sample the root bone as well as the child -- so the gate is known to be able to fail for both |
| CNB's writers produce identical bytes on both backends | CNA-TS | the content-pipeline boundary claims a .cnb built in a build script and one built in a page are the same bytes. A browser page builds the same 52-byte payload and the same 326-byte container from the same calls, and the test compares the payload against the exact little-endian layout the desktop ABI produces rather than against the page agreeing with itself. The read-limits structure crosses through a wasm32 layout measured by an Emscripten probe, not a handwritten one |
| Modern CNA runtime services over WebAssembly | cna-ts/extensions/runtime | the browser harness reads platform, renderer selection and available renderers, and round-trips two by-value CNA_StringView routes |
| WebAssembly game lifecycle, device creation, Clear, Texture2D transfer, SpriteBatch and input snapshots | CNA-TS | test/wasm-browser.mjs drives the first vertical slice through the same public XNA classes the Node backend serves |

## EXPLICITLY_UNAVAILABLE_WITH_CURRENT_BACKEND

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Compiled-effect execution on the qualified renderer | current qualified backend | CNA capability bit reports compiled effects unavailable |
| ContentLost event delivery | current qualified backend | ABI 0.9 raises the event for real on the renderers whose API can lose a device (DirectX9, Direct2D, Skia); HEADLESS has no device to lose, so the producer never runs and the native suite asserts zero raises rather than pretending otherwise |
| GraphicsAdapter discovery, current display mode and windowed adapter capabilities | current qualified backend | HEADLESS artifact exposes no windowed adapter/display evidence |
| Physical GameWindow resize/orientation/screen-device event qualification | current qualified backend | event registrations are real and removable, but HEADLESS exposes no physical window or event stimulus |
| Texture3D and TextureCube resource execution on the qualified renderer | current qualified backend | both exact ABI-0.7 creation routes return CNA_RESULT_NOT_SUPPORTED under HEADLESS; no storage or transfer success is claimed |

## UPSTREAM_CNA_BLOCKED

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Exiting a process after cna_game_destroy has refused | CNA | a refused cna_game_destroy is not side-effect-free: a process that ends in that state takes SIGSEGV after its last statement has run, and an explicit process.exit(0) does not avoid it. Measured five ways in one shape -- a game never destroyed exits cleanly, a game destroyed exits cleanly, a refusal that is cleaned up and retried exits cleanly, and a refusal that is left standing crashes whether or not the offending handle was released afterwards -- so the crash belongs to the refusal rather than to a live game at exit. Written up as upstream finding 18. Every probe game in this package destroys its game and asserts the destroy succeeded, and the suites run one game per process, so a regression that starts refusing shows up as a failed assertion and as a non-zero exit code rather than as a passing file that crashes on the way out |
| GPU instance culling | CNA | the culler runs and keeps everything. Its compute shader tests each instance against the camera's six frustum planes and increments a visible count the CPU uploaded as zero; the count comes back as the instance count in every case, including three instances ten thousand units outside a hundred-unit frustum. Measured on OPENGLES3, the only renderer built here that supports it -- HEADLESS, SOFTWARE and SDL_RENDERER answer NOT_SUPPORTED and the question does not arise. Nothing is announced: is_supported answers true and the unsupported reason is empty, so a game that adopts it pays for the dispatch, the upload and the readback and draws what it would have drawn anyway. Written up as upstream finding 20, with this package's own BoundingFrustum computing the expected count beside each measured one, so the two cullers are compared with each other. One planted defect -- ignoring an instance's world transform -- survives, and that is recorded rather than hidden: while the cull rejects nothing, whether the transform arrived cannot be observed from outside. A repaired culler kills that mutation and fails two of the four asserted rows, which is the point of asserting them |
| Handing a post-process pass to a chain with PostProcessChain.AddOwned | cna-ts/extensions/graphics | cna_post_process_chain_add_owned_pass consumes the handle without the RemoveOwnedGraphicsResourceFor its sibling _destroy performs, so the game's owned-graphics-resource counter never falls and every later cna_game_destroy in the process refuses with CNA_RESULT_INVALID_STATE; measured and written up in docs/upstream-cna-findings.md, characterised in its own process by test/post-process-owned-pass.probe.mjs |
| Reading a PBR effect's texture slots back | CNA | the slots have two sources of truth and they never agree. cna_pbr_effect_apply_material writes the C++ effect's texture pointers; cna_pbr_effect_set_texture writes the C API's own retained-handle table, which is the only thing cna_pbr_effect_get_texture reads. Measured identically on HEADLESS and OPENGLES3: a texture applied with a material is invisible to the slot getter, a texture placed through the setter is invisible to the extractor, and applying a material with an empty slot does not clear one the setter filled. So a material carrying a texture cannot be round-tripped through an effect. Written up as upstream finding 19; all three rows are asserted in both suites, and the binding documents on ExtractMaterial that the slots come back empty rather than inventing a Texture2D wrapper for a handle it does not own |
| The camera use-after-free CNA has today | CNA | destroying a test-backend camera and then opening the platform one is a segmentation fault in CNA 0.21.0: the C ABI installs the test provider as a process-wide platform override holding a raw pointer into the camera resource, and cna_camera_destroy frees that resource without clearing the override. Reproduced in plain C with no binding involved, backtrace resolved to CNA::Devices::Camera::Camera at Camera.cpp:68. cna-ts runs the crashing sequence in a child process and asserts the child dies, so a repair fails the test rather than passing unnoticed; every probe here that opens the platform camera runs before the first test-backend one |

## FIXTURE_PENDING

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| AudioEngine, AudioCategory, WaveBank, SoundBank and Cue authored success | qualification fixtures | native routes and invalid/missing-bank behavior verified; no legal redistributable XGS/XSB/XWB corpus |
| MediaLibrary albums/artists/genres/playlists and picture streams | qualification fixtures | strict shape exists but no deterministic populated media-library fixture |
| VideoPlayer.Play decode, duration and frame progression | qualification fixtures | native controls verified; no legal decode fixture qualified |

## HARDWARE_PENDING

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| GPU custom-effect and rendering output qualification | qualification hardware | typed bound/user/instanced draw routes now reach CNA, but the qualified HEADLESS pipeline returns result 12 and cannot prove visible output |
| Microphone enumeration/capture/buffer callbacks | qualification hardware | all routes imported; HEADLESS enumerates zero microphones |
| Physical joystick and force-feedback hardware | cna-ts/extensions/input | no joystick and no haptic device are attached to this host, so CNA reports zero of each. The absence contract is verified natively and the data path is verified through the boundary, but no reading has come off a real stick or a real motor here. That is stated rather than implied: a green suite on a device-less machine is evidence about absence, not about hardware |
| Physical keyboard/mouse/gamepad/touch behavior | qualification hardware | HEADLESS route execution is deterministic but not physical-device evidence |
| SoundEffect/MediaPlayer audible output | qualification hardware | NULL audio verifies state/lifetime only |
| The gyroscope's reading path, and physical compass/motion hardware | cna-ts/extensions/sensors | The gyroscope is projected and its absence contract is verified, but its *reading* path cannot be exercised here: CNA gives the compass and the motion sensor a synthetic test backend and gives the gyroscope only a support override, so Start still needs a real platform sensor service and this host has none. Recorded as upstream finding 6 rather than worked around. The compass and motion data paths are proved by injection, which is not a measurement of physical hardware either; no reading in this package has come off a real sensor |

## PLATFORM_PENDING

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Android and iOS runtime | platform qualification | not attempted; no mobile artifact/toolchain evidence |
| Electron desktop runtime | platform qualification | not attempted and no windowed artifact |
| Windows and macOS Node/native runtime | platform qualification | no qualified artifact/job on either OS |

## UNIMPLEMENTED_CNA_TS

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| CNB's loader registry, and the native content manager it needs | CNA-TS | measured rather than deferred on style. The registry itself works: a C probe mints a custom asset type id from a name, registers a loader, writes a .cnb of that type, and cna_content_manager_load_foreign_ext calls the loader and hands the object back -- twice for the same name, from the cache, without calling the loader again, which is XNA reference-type caching. What blocks the projection is this binding's own architecture: that retrieval route takes a NATIVE content manager handle, and there is none here. ContentManager in this package is a managed XNB reader that opens the file through TitleContainer and constructs the object in TypeScript, and no cna_content_manager_* route is imported at all. Registering a loader whose product could never be retrieved would be a mechanism with no consumer. Adopting it means projecting CNA's own ContentManager as a second content path with its own cache, which is an architectural decision to take deliberately rather than as a side effect of wanting one registry. The callback shape is not the obstacle: a trampoline holding a napi_ref and a checked slot table would keep void* and function pointers entirely inside the bridge, the same way the text-input subscriptions already do |
| GamerServices sign-in, achievements, leaderboards, avatars and the whole .Net session surface | CNA-TS | the dispatcher's lifetime, the Guide's state and both of the Guide's asynchronous screens are projected and verified above; everything that needs a signed-in user, a friends list or a peer -- sign-in, achievements, leaderboards, presence, avatars and every NetworkSession operation -- still refuses with GamerServicesNotAvailableException, because a fabricated gamer or peer would be worse than the exception XNA itself raises where the platform is absent |

## LANGUAGE_MAPPING_LIMITATION

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Arbitrary custom generic vertex value transfer | TypeScript language projection | four formally mapped XNA vertex structs have exact codecs; arbitrary JavaScript objects have no blittable CLR layout and are rejected explicitly |
| ContentReader.ReadRawObject<T>() without an explicit reader token | TypeScript language projection | JavaScript erases T, so reader identity cannot be recovered deterministically; explicit ContentTypeReader overloads are implemented and the strict verifier models the generic surface without fake CLR reflection |
| DrawUserIndexedPrimitives Int16[] versus Int32[] overload identity | TypeScript language projection | both CLR element types map to number[]; the deterministic public number[] route uses 32-bit indices rather than guessing from values |

## NOT_APPLICABLE_TO_SELECTED_ENVIRONMENT

| Operation family | Owner/boundary | Evidence |
| --- | --- | --- |
| Windowed renderer behavior on Linux | selected environment | qualified artifact is HEADLESS |
